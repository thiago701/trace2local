# Gerado pelo instalador do TraceVanta (mvn tracevanta:configure)
tracevanta:
  port: 9876            # UI do TraceVanta
  redaction:
    mode: strict        # strict | keys | off
  retention:
    max-executions: 100
  aws:
    dynamodb:
      capture-before: true
